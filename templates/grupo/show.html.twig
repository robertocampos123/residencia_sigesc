{% extends 'base.html.twig' %}

{% block title %}Grupo{% endblock %}

{% block body %}
    <h1>Grupo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupo.id }}</td>
            </tr>
            <tr>
                <th>Clave_grupo</th>
                <td>{{ grupo.claveGrupo }}</td>
            </tr>
            <tr>
                <th>Cupo</th>
                <td>{{ grupo.cupo }}</td>
            </tr>
            <tr>
                <th>Inscritos</th>
                <td>{{ grupo.inscritos }}</td>
            </tr>
            <tr>
                <th>Aula</th>
                <td>{{ grupo.aula }}</td>
            </tr>
            <tr>
                <th>Horario</th>
                <td>{{ grupo.horario }}</td>
            </tr>
            <tr>
                <th>Instrumentacion_didactica</th>
                <td>{{ grupo.instrumentacionDidactica }}</td>
            </tr>
            <tr>
                <th>Avance_programatico</th>
                <td>{{ grupo.avanceProgramatico }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grupo_index') }}">back to list</a>

    <a href="{{ path('app_grupo_edit', {'id': grupo.id}) }}">edit</a>

    {{ include('grupo/_delete_form.html.twig') }}
{% endblock %}
