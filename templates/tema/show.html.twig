{% extends 'base.html.twig' %}

{% block title %}Tema{% endblock %}

{% block body %}
    <h1>Tema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tema.id }}</td>
            </tr>
            <tr>
                <th>Numero_unidad</th>
                <td>{{ tema.numeroUnidad }}</td>
            </tr>
            <tr>
                <th>Nombre_unidad</th>
                <td>{{ tema.nombreUnidad }}</td>
            </tr>
            <tr>
                <th>Subtemas</th>
                <td>{{ tema.subtemas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tema_index') }}">back to list</a>

    <a href="{{ path('app_tema_edit', {'id': tema.id}) }}">edit</a>

    {{ include('tema/_delete_form.html.twig') }}
{% endblock %}
